/**
 * Page-specific hooks
 *
 * These hooks contain the data-fetching and state management logic for specific pages.
 * Each hook is named after the page it supports.
 */

// Export all page hooks
export { useHomePage } from "./useHomePage";
export { useMovieDetailPage } from "./useMovieDetailPage";
export { useLikedPage } from "./useLikedPage";

// Add additional page hooks as they are created
